import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    allowedDevOrigins: ["localhost",
        "100.70.24.30", "msi-eulysis", "100.119.3.44", "msi-jake"
    ],
};

export default nextConfig;
